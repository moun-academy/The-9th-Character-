import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mounacademy.the9thcharacter',
  appName: 'The 9th Character',
  webDir: 'dist'
};

export default config;
